@model text1


@using (Html.BeginForm("Analyze", "TextAnalyticsSummary", FormMethod.Post))
{
    @Html.TextAreaFor(m => m.InputText, new { @class = "form-control" })
    @Html.ValidationMessageFor(m => m.InputText)

    <button type="submit" class="btn btn-primary">Analyze Text</button>
}
